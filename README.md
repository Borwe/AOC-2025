# My C solutions to Advent Of Code 2025

## Usage
First setup
```sh
meson setup build
```

Then to run, pass the day`x`_p`y`, replacing `x` with day, and `y` with part. Eg to run day1 part 2 do:
```sh
meson compile day1_p2 -C build
```
